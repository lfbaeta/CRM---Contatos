HOST=127.0.0.1
PORT=3000
# npm run setup:local gera as credenciais para o PostgreSQL do compose.yaml.
# Para PostgreSQL instalado: altere a URL para o usuário, senha, porta e banco reais.
DATABASE_URL=postgresql://user:password@127.0.0.1:5432/crm_contatos?sslmode=disable
CRM_API_TOKEN=
